# Telegram (servidor — NUNCA use prefixo VITE_, fica exposto no browser)
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Opcional: URL da API em produção (para testar frontend local contra API deployada)
# VITE_WAITLIST_API_URL=https://seu-dominio.vercel.app/api/waitlist
